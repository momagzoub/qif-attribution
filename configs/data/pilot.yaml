dataset_name: qif_pilot
manifest_path: data/manifests/example_manifest.csv
image_root: data/raw
split:
  train_fraction: 0.7
  val_fraction: 0.15
  test_fraction: 0.15
  group_by: prompt_id
  seed: 1729
required_labels:
  - prompt_source
  - generator_id
  - prompt_style
